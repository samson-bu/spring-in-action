<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.access.orm.mybatis.mapper.PeopleMapper">
    <select id="getPeople" resultType="data.access.entity.People">
    select * from test_table where id = #{strId}
  </select>
</mapper>